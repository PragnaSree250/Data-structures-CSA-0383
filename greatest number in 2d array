#include<stdio.h>
int main()
{
	int size,a[100],i,max=0;
	scanf("%d",&size);
	for(i=0;i<size;i++)
	{
		scanf("%d",&a[i]);
	}
	max=max+a[0];
	for(i=0;i<size;i++)
	{
		if(max<a[i])
		{
			max=a[i];
		
		}
	}
	
	printf("max :%d",max);	
	return 0;
	}	
